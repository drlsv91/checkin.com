apiVersion: v1
kind: Service
metadata:
  name: {{ include "checkin.fullname" . }}
  labels:
    {{- include "checkin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "checkin.selectorLabels" . | nindent 4 }}
