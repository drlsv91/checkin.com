apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "checkin.fullname" . }}-test-connection"
  labels:
    {{- include "checkin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "checkin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
